# Showcase pairs

Drop before/after image pairs here (JPG or PNG, ~1200px on the long side).
Then add an entry to `src/data/showcase.ts` pointing at each pair.

Until at least one pair exists, the landing page shows a single honest
placeholder card instead of empty grey boxes.
